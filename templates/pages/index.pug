doctype html
html(lang=lang.key)
  head
    if debug
      include ../includes/head-debug.pug
    else
      include ../includes/head.pug
  body.no-skin
    // Here be the content:
    // Main TEXT
    // Download Manuals
    // Link to App (app.xxx.de)
    if debug
      include ../includes/footer-debug.pug
    else
      include ../includes/footer.pug
